{#
  config.php.j2
  Managed by ansible-role-sendy_install. Manual edits will be overwritten
  on the next run of this role.

  Field names verified 2026-07-04 against a real production
  includes/config.php (Sendy 7.0.6). Sendy is closed-source — re-verify
  against includes/config.php.example if a future Sendy release renames
  or adds constants.
#}
<?php

define('APP_PATH', '{{ sendy_install_url }}');

$dbHost = '{{ sendy_install_db_host }}';
$dbUser = '{{ sendy_install_db_username }}';
$dbPass = '{{ sendy_install_db_password }}';
$dbName = '{{ sendy_install_db_name }}';

$charset = '{{ sendy_install_db_charset }}';
$dbPort = {{ sendy_install_db_port }};

define('COOKIE_DOMAIN', '{{ sendy_install_cookie_domain }}');
